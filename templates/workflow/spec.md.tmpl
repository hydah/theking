# {task_title} Spec

<!-- All sections below are required, even for lightweight tasks.
     Brevity is allowed; omission is not. -->

## Scope
<!-- Define the smallest deliverable that can pass review.
     Be specific: which files, which functions, which user-visible behavior. -->

## Non-Goals
<!-- Explicitly state what this task does NOT cover.
     Prevents scope creep during implementation. -->

## Acceptance
<!-- Every checkbox must declare how it is verified and where the evidence
     lives. Allowed 验证方式 values: unit | integration | smoke | e2e |
     manual-observed. Evidence path can be a file under verification/ or
     a test identifier like tests/test_x.py::test_y. -->
- [ ] <!-- Specific, testable criterion. Each should be independently verifiable. -->
  - 验证方式: <!-- unit | integration | smoke | e2e | manual-observed -->
  - 证据路径: <!-- verification/<profile>/smoke.md or tests/test_x.py::test_y -->

## Test Plan
{test_plan}

## Edge Cases
<!-- sprint-017 TASK-003: Edge Cases supports two structures.

  Preferred (new): split into two subsections so the adversarial-inputs
  discipline from tdd-guide ('enumerate failure categories, cover many')
  becomes machine-enforced. Full flow requires >= 2 Failure modes and
  >= 1 Happy variant; lightweight requires >= 1 Failure mode.

  Legacy (still accepted): a flat bullet list under '## Edge Cases'
  with no '###' subsections. Uses the aggregate threshold (full >= 3,
  lightweight >= 1).

  Mixing both forms (top-level bullets AND subsection headers) is
  rejected; pick one structure.

  List boundary conditions, error scenarios, and unusual inputs to handle. -->

### Failure modes
<!-- Inputs / conditions under which the change SHOULD break or reject.
     Null / boundary / concurrency / ordering / encoding categories
     are typical starting points. -->

### Happy variants
<!-- Non-canonical but still-expected-to-succeed input shapes. Often
     the tests discover these when thinking about what the feature
     should tolerate. -->
