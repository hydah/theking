# Follow-ups for {sprint_name}

<!-- This file is append-only metadata, written by `workflowctl followup-sprint`.
     Each bullet records a downstream sprint that was raised against this one.
     Do not delete or reorder entries — they form an audit trail back to the
     work that prompted the followup. -->
