---
id: {task_id}
title: {task_title}
status: draft
status_history:
  - draft
task_type: {task_type}
execution_profile: {execution_profile}
verification_profile:
{verification_profile_block}
requires_security_review: {requires_security_review}
required_agents:
{required_agents_block}
depends_on:
{depends_on_block}
{bundle_block}current_review_round: 0
---

## Goal
<!-- Describe the expected OUTCOME, not the implementation.
     Example: "Users can log in via OAuth and see their personal dashboard." -->

## Constraints
- Keep scope minimal until the current review round is closed.
- Update status and status_history on every state change.
- When status enters `in_review`, set current_review_round to the new round number.
- Do not start implementation before spec.md is filled in and task reaches `planned`.

<!-- Optional: add `flow: lightweight` to the frontmatter above to lower the
     spec gate thresholds (Test Plan >= 3, Edge Cases >= 1) for small,
     single-deliverable tasks. Default is `flow: full` (Test Plan >= 5,
     Edge Cases >= 3). The setting is preserved across status transitions.
     See `.theking/agents/planner.md` for when to use lightweight flow. -->

