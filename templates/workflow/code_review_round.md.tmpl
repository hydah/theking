# {review_label} Review Round {round_number}

## Context
- Task: {task_id}
- Summary: describe what changed in this round.
- Reviewer: <name>
- Reviewer independence: <self | subagent-via-task-tool | subagent-via-cli | main-agent-fallback>

<!-- Reviewer independence declares who audited the diff. For new terminal
     tasks, self and main-agent-fallback do not satisfy independent review;
     use subagent-via-task-tool or subagent-via-cli and record matching
     reviewer-agent provenance in agent-runs.jsonl. Legacy self/fallback
     review still requires a Self-audit checklist with >= 10 top-level
     bullets for compatibility with older artifacts. -->

## Findings

<!-- Every finding MUST carry a stable id of the form `finding-NNN`.
     The paired `.resolved.md` file must contain a matching `### finding-NNN`
     subsection for each id below, with `- Status: resolved|waived`.
     `workflowctl check` enforces this via validate_review_resolution_coverage.

     If this round has no findings, delete the scaffold below and write:
         - (no findings this round)
     That legacy prose form is still accepted (coverage gate is a no-op when
     no `### finding-` headers are present). -->

### finding-001 · <severity> · <short title>
- Location: <path/to/file.ext:line>
- Severity: <blocker | major | minor | nit>
- Finding: <what is wrong and why it matters>

<!-- Add additional findings as ### finding-002, ### finding-003, ...
     Ids do not need to be consecutive; the gate only checks that every id
     declared here is closed in the paired resolved file. -->

## Self-audit checklist (>= 10 points; required when Reviewer independence: self or main-agent-fallback)

<!-- When reviewer is the main agent itself, compensate for the loss of
     adversarial pressure by enumerating at least 10 top-level checks
     (e.g. "Scope adherence", "TDD integrity", "No opt-out flags",
     "Error message quality", ...). Only top-level bullets count.
     Nested bullets are permitted but don't contribute to the floor.

     When Reviewer independence is subagent-via-task-tool or
     subagent-via-cli, this section is optional. -->
- 
