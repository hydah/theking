# Resolved Code Review Round {round_number}

<!-- This template is the authoring reference for reviewers/modifiers writing
     `<type>-review-round-NNN.resolved.md`. It is NOT auto-generated by
     `workflowctl init-review-round` (only the review `.md` is). Copy the
     structure below when you close findings.

     Contract enforced by `workflowctl check`
     (validate_review_resolution_coverage):
       - Every `### finding-NNN` declared in the paired review round must
         appear below with `- Status:` equal to `resolved` or `waived`.
       - `Status: waived` additionally requires a non-empty
         `- Waiver-Reason:` line.
       - Legacy prose format (no `### finding-` headers in either file)
         remains accepted; the gate is a no-op for those rounds. -->

## Fixes

### finding-001
- Status: resolved
- Fix: <what changed, pointing at commits/files>
- Evidence: <tests/test_x.py::test_y or verification/<profile>/smoke.md>

<!-- Example of a waived finding:

### finding-002
- Status: waived
- Fix: (not applied)
- Evidence: n/a
- Waiver-Reason: out of scope for this task; tracked in FOLLOWUPS/...

Add one `### finding-NNN` block per finding id declared in the review round. -->

## Verification
- List the commands or checks used to confirm the fixes.
