# {project_title} Overview

## 一句话说明

{project_title} 使用 `.theking/` 作为 canonical project KB。请在这里补充它服务的用户、核心问题和主要目标。

## 负责范围

- 维护项目内 canonical docs 于 `.theking/`
- 在 `.theking/workflows/{project_slug}/` 下沉淀 project / sprint / task 工件
- 用 `execution_profile` 与 `verification_profile` 约束验证路径
- 让 review、verification 和状态机规则可被 workflowctl 校验

## 不负责范围

- 不替代项目自己的源码、运行脚本或部署系统
- 不把临时运行日志直接当作长期知识
- 不假设项目已经 vendor theking 的 runtime 入口

## 项目入口

- 项目知识入口：[.theking/README.md](../README.md)
- 代码主入口：请填写当前项目的源码入口
- 运行时入口：请填写当前项目的启动命令或包装入口
- 测试入口：请填写当前项目的测试命令或测试目录

## 关键术语

- `.theking`：项目内 canonical governance root
- `workflow root`：传给 `workflowctl --root` 的项目父目录
- `execution_profile`：任务实际运行环境，例如 `web.browser`、`backend.http`
- `verification_profile`：任务必须满足的验证画像列表