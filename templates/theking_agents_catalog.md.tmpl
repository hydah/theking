# Agent Catalog

这里记录 {project_slug} 当前纳管的 agent、职责边界和外部来源。

## 默认纳管 agent

| agent | 用途 | 默认触发时机 | 当前处理方式 |
|------|------|------|------|
| `planner` | 复杂特性规划 | 进入实现前 | catalog 化 |
| `tdd-guide` | 测试先行工作流 | 新功能、bug 修复、合同重构 | catalog 化 |
| `code-reviewer` | 改动后代码审查 | 每次代码改动完成后 | catalog 化 |
| `e2e-runner` | 浏览器与关键路径 e2e | `web.browser` 画像任务 | catalog 化 |
| `security-reviewer` | 安全审查 | `backend.http` 或 `auth` / `input` / `api` 相关任务 | catalog 化 |

## 维护原则

- 先沉淀 agent 名称、职责、触发条件和输入输出合同
- 如需完全离线自包含，再补充 `vendored/` 或 `sources/` 子目录
- 外部运行时配置可以引用这里，但不能反向成为真相源