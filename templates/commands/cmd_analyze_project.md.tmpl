---
description: 分析项目源码，填充 .theking/context/ 知识库（规则以 knowledge-base skill 为准）
---

# Analyze Project

扫描项目结构和源码，填充 `.theking/context/` 下的知识文档。

本命令只做**显式入口**。**分析步骤、写入规则、PLACEHOLDER 填充流程、context/ vs memory/ 的边界**一律以 `.theking/skills/knowledge-base/SKILL.md` 为准，避免两处描述漂移。

## Usage

```
/analyze-project            # 等同于 full，首次使用或有 PLACEHOLDER 时跑
/analyze-project full        # 全量扫描，填充所有 PLACEHOLDER
/analyze-project incremental # 增量更新，仅补充新发现，不覆盖手写内容
```

## 执行时的唯一动作

1. 读取 `.theking/skills/knowledge-base/SKILL.md`
2. 按 skill 中「PLACEHOLDER 检测与自动填充」章节的流程执行
3. 分析原则、写入位置、知识生命周期**全部**遵循该 skill

## 没有 slash command 的工具怎么用

Cursor / Codex / CodeBuddy 等不支持 slash command 的工具，直接对 agent 说：

> 按 `knowledge-base` skill 的流程分析本项目并填充 `.theking/context/`

效果与 `/analyze-project` 完全等价。
