---
description: "皇帝下旨 — 从需求到交付的全流程编排。用法：/decree <需求描述>"
---

# 👑 Decree — 奉旨开发

皇帝一道旨意下去，群臣领旨办事。

**治理规则与朝廷架构**以 `.theking/skills/workflow-governance/SKILL.md` 为准，本命令只管编排。

以下 `workflowctl` 命令默认在项目根目录执行，并依赖本机已安装的 `workflowctl` 入口。优先把 `--project-dir` 传项目根目录 `.`；`--project-dir` 要求项目目录名和 `--project-slug` 精确一致。旧脚本仍可继续使用 `--root <项目父目录>`，如果你手上只有 `.theking/` 路径，传 `--project-dir .theking` 即可。

## Usage

```
/decree <需求描述>
```

示例：
```
/decree 实现用户登录功能，支持 OAuth 和手机验证码
/decree 重构数据库连接池，支持读写分离
/decree 给 dashboard 加上实时通知
/decree 修复登录页 token 过期不刷新的 bug
```

## Compact Recovery

如果会话刚做过 compact、切到新工具，或你不确定这道旨意做到哪一步，先恢复状态，再继续执行：

```bash
{theking_cmd} status --project-dir {workflow_root_quoted} --project-slug {project_slug}
```

如果 `status` 显示仍有 decree checkpoint 或 active task，先沿着输出的 next step 继续，不要重新发起一条新的 decree。

## 流程

收到旨意后，按以下步骤自动编排。**所有需求都必须先建 sprint，再写代码，无例外。**

### Phase 1: 审上下文 — 最小初勘

先找证据，再决定流程级别。至少完成以下动作：

1. 搜索并阅读直接相关的代码、测试、文档、报错、接口或数据契约
2. 标出影响面：涉及哪些文件、模块、用户流程、外部接口
3. 标记风险：是否触发 auth、input、api、web.browser、schema/config/public contract 变化
4. 输出仍不确定的点；只要存在关键未知项，后续一律按完整流程处理

输出格式固定如下：

```
👑 [decree] 上下文初勘：
- 已查看：<代码/测试/文档/报错>
- 影响面：<模块/接口/用户流程>
- 风险标签：<无 / auth / input / api / web.browser / ...>
- 未决问题：<无 / 列表>
```

**⛔ 禁止在完成最小上下文初勘前宣告“轻量流程”，也禁止开始实现代码。**

---

### Phase 2: 审旨 — 分流决策

基于已查看的证据，选择流程级别（标准见 workflow-governance）：

**完整流程**（常见信号：3+ 文件、架构决策、> 30 分钟，或存在关键未知项）：
```
👑 [decree] 此旨意走完整流程。
理由：<基于上下文初勘的证据>
下一步：召中书令（planner）谋划，创建 sprint。
```

**轻量流程**（只在已完成初勘且能确认单一变更单元时成立）：
```
👑 [decree] 此旨意走轻量流程。
理由：<基于上下文初勘的证据>
下一步：创建单 task sprint。
```

**⛔ 若仍有关键未知项，或执行中发现隐藏耦合/验证范围扩大，必须升级到完整流程。**

分流结论一旦做出，立刻落盘 checkpoint，防止 compact 后丢失后续编排：

```bash
{theking_cmd} checkpoint --project-dir {workflow_root_quoted} --project-slug {project_slug} --phase phase-2-triage --flow <full|lightweight> --summary "<一行旨意摘要>" --next-step "<下一步动作>"
```

---

### Phase 3: 谋划 — Sprint 规划（必须完成才能写代码）

先执行 Bootstrap（幂等）：

```bash
{theking_cmd} ensure --project-dir {workflow_root_quoted} --project-slug {project_slug}
```

#### 完整流程

1. 召 **中书令**（planner）起草方略，拆解需求为 task 列表
2. 涉及架构决策时，先问 **太傅**（architect）
3. 确认 task 间依赖关系（DAG）
4. 创建 sprint + 批量创建 task：
   ```bash
   {theking_cmd} init-sprint --project-dir {workflow_root_quoted} --project-slug {project_slug} --theme <theme>
   {theking_cmd} init-sprint-plan --project-dir {workflow_root_quoted} --project-slug {project_slug} --sprint <sprint> --plan-file plan.json
   ```
5. 输出 Task Overview 表和依赖图，呈御览

#### 轻量流程

1. 创建单 task sprint（跳过 planner）：
   ```bash
   {theking_cmd} init-sprint --project-dir {workflow_root_quoted} --project-slug {project_slug} --theme <theme>
   {theking_cmd} init-task --project-dir {workflow_root_quoted} --project-slug {project_slug} --sprint <sprint> --slug <slug> --title <title> --task-type <type>
   ```

**✅ 检查点：sprint 和 task 已创建，才能进入 Phase 4。**

创建完 sprint / task 后，立即刷新 checkpoint：

```bash
{theking_cmd} checkpoint --project-dir {workflow_root_quoted} --project-slug {project_slug} --phase phase-3-planning --flow <full|lightweight> --summary "<同一条旨意摘要>" --sprint <SPRINT> --task-dir <TASK_DIR> --next-step "<下一步动作>"
```

---

### Phase 4: 领旨 — 开发

按依赖图拓扑排序，无依赖的 task **并行启动**（轻量流程只有 1 个 task）。

对每个 task：

1. **激活**
   ```bash
   {theking_cmd} activate --task-dir <TASK_DIR>
   ```

2. **写 Spec**
   - 完整流程与轻量流程都使用同一模板
   - Scope、Non-Goals、Acceptance、Test Plan、Edge Cases 全部 section 都必须保留
   - 轻量流程可以短写，但不得留空、不得省略章节

3. **TDD 循环** — 召 **门下侍中**（tdd-guide）封驳审核：先写测试 → 最小实现 → 重构

4. **基础验证** — 在进入 review 前，必须运行与改动相关的 build/lint/type/unit checks，并补齐执行画像要求的验证：
   - `web.browser`：至少做一次真实页面 smoke 或轻量 e2e
   - `backend.http`：至少做一次真实请求或集成式验证
   - `backend.cli` / `backend.job`：至少执行真实入口并核对输出或副作用
   - 将命令、结果、截图/日志等证据写入 `verification/<profile>/`

5. **Review** — 按 workflow-governance agent 触发规则：
   - **御史大夫**（code-reviewer）— 必须
   - **刑部尚书**（security-reviewer）— auth/input/api 或 backend.http 时
   - **兵部尚书**（e2e-runner）— web.browser 时
   - 有 CRITICAL/HIGH findings → 修复 → 再 review

6. **构建失败** → 召 **工部尚书**（build-error-resolver）排障修复

7. **性能问题** → 召 **户部尚书**（perf-optimizer）度支钱粮

8. **校验**
   ```bash
   {theking_cmd} check --task-dir <TASK_DIR>
   ```

9. **完成** — status → `done`，但只在 review pair 与 verification evidence 完整后进入完成态

---

### Phase 5: 收旨 — 收尾

1. Sprint 级校验：
   ```bash
   {theking_cmd} sprint-check --sprint-dir <SPRINT_DIR>
   ```
2. 完整流程：召 **吏部尚书**（refactor-cleaner）裁汰冗余代码
3. 知识提炼 — 有复用价值的分析产物提炼到 `.theking/context/`
4. 完整流程：召 **礼部尚书**（doc-updater）修史编典
5. 停用：
   ```bash
   {theking_cmd} deactivate --project-dir {project_dir_quoted}
   ```
6. 汇报旨意执行结果
