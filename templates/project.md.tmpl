# {project_title}

## Mission
- Define the product goal, non-goals, and hard constraints for {project_slug}.

## Success Criteria
- Every task is linked to an explicit spec, review round, and acceptance check.
- Agent execution stays inside the project -> sprint -> task -> review hierarchy.

## Governance
- Planner runs before any implementation work.
- TDD stays strict: red, green, refactor.
- ready_to_merge or done requires review pairs from round 001 through current_review_round.
