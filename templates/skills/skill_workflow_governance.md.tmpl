---
name: workflow-governance
description: |
  theking 开发工作流治理 — 状态机、分流决策、硬规则、review loop、agent 触发。
  触发词：开发工作流、sprint、task、spec、review、TDD、状态机、agent 协作。
---

# Workflow Governance

本 skill 定义 {project_title} 的开发工作流纪律。所有 AI agent 在执行编码任务时必须遵守以下规则。

## 核心原则

**所有需求都通过 sprint 跟踪**，无论大小。区别在于流程轻重，不在于是否跟踪。

**先审上下文，再做分流。** 没看相关代码、测试、文档、报错或接口契约之前，不得宣告“轻量流程”。

**轻量流程只减少规划开销，不降低交付标准。** spec、TDD、review、构建/测试/验证、check/sprint-check 的底线一致。

## 分流决策（完成最小上下文初勘后）

收到任务后，先完成最小上下文初勘，再评估流程级别。最小上下文初勘至少包括：

- 阅读相关代码、测试、文档、错误日志或接口定义中的直接证据
- 标记影响面：文件、模块、接口、数据契约、用户流程
- 识别风险标签：auth、input、api、web.browser、schema/config/public contract
- 输出仍不确定的点；只要存在关键未知项，默认升级到完整流程

分流时必须给出“上下文初勘 + 流程结论”，禁止只凭需求描述或预计文件数做主观判断：

```
👑 [decree] 上下文初勘：
- 已查看：<代码/测试/文档/报错>
- 影响面：<模块/接口/用户流程>
- 风险标签：<无 / auth / input / api / web.browser / ...>

👑 [decree] 此旨意走<完整|轻量>流程。
理由：<基于已查看证据的判断>
```

### 完整流程

条件：出现以下任一情况就走完整流程：

- 影响面跨 3+ 文件、跨模块或跨团队契约
- 需要架构决策、数据迁移、配置变更或公共接口变更
- 存在多 task 依赖、回滚复杂度高，或关键未知项尚未排清
- 触发高风险画像，且需要多阶段验证才能确认结果

- planner 拆解为多 task + 依赖图
- 每个 task 写完整 spec（所有 section 都保留）
- 多轮 review loop
- 知识提炼

### 轻量流程

只有在完成初勘后，同时满足以下条件，才允许走轻量流程：

- 能确认是单一变更单元，单 task 即可闭环
- 无 schema/config/public contract 变化，无额外架构分叉
- 已有现成实现模式可复用，不需要 planner 拆多 task
- 风险与验证路径明确，不存在关键未知项

- 单 task sprint（跳过 planner）
- spec 仍使用同一模板、保留全部 section，但每节可短写
- 通常 1 轮 review
- 知识提炼按需

若轻量流程执行中发现隐藏耦合、风险扩大或验证不足，必须立即升级为完整流程，不能硬撑到底。

### 对比表

| 环节 | 完整流程 | 轻量流程 |
|------|---------|---------|
| 上下文初勘 | **必须** | **必须** |
| 创建 sprint + task | ✓ 多 task | ✓ 单 task |
| planner 拆解 | ✓ | 跳过 |
| spec.md | 同模板全部 section，详细填写 | 同模板全部 section，可短写但不得省略 |
| tdd-guide | **必须** | **必须** |
| build/lint/type/unit + 画像验证 | **必须** | **必须** |
| code-reviewer | **必须** | **必须** |
| security/e2e review | 条件触发 | 条件触发 |
| review loop | 至少 1 轮，可能多轮 | 至少 1 轮，通常 1 轮 |
| check + sprint-check | **必须** | **必须** |
| 知识提炼 | ✓ | 按需 |

**不变的底线**：无论哪种流程，sprint 跟踪、完整 spec、TDD、code-review、基本验证、artifact 校验都不可绕过。

## 中断恢复

任何新会话、compact 后恢复、或切换到另一个 AI 工具时，第一步都不是重新理解需求，而是先恢复当前 decree / task 状态：

```bash
{theking_cmd} status --project-dir {workflow_root_quoted} --project-slug {project_slug}
```

如果已经做出分流结论但还没完成 sprint / task 编排，必须把后续动作落到 decree checkpoint；如果已经有 active task，则优先沿 active task 继续。

## 硬规则

1. **所有需求都有 sprint** — sprint 是最小跟踪单元，不可绕过
2. **先审上下文，再做分流** — 禁止仅凭需求描述、文件数猜测或主观“简单/复杂”判流程
3. **先红后绿** — 实现前先启动 tdd-guide（门下侍中），必要时补 e2e-runner（兵部尚书）
4. **每次改动必须 review** — 御史大夫（code-reviewer）是必经之路；触及 auth/input/api 或 `backend.http` 画像任务，额外召刑部尚书（security-reviewer）
5. **没有完整 spec 不进入实现** — 轻量流程也要保留 Scope、Non-Goals、Acceptance、Test Plan、Edge Cases 全部 section
6. **没有基本验证不进入 ready_to_merge/done** — 必须运行与改动相关的 build/lint/type/unit 检查，并补齐执行画像要求的验证；无自动化时需明确记录缺口与最小人工验证
7. **没有 resolved review 或校验证据不进入 done** — review pair 与 verification evidence 必须完整
8. **任何 compact 或换工具恢复都先跑 `workflowctl status`** — 先恢复 decree / task 的 durable 状态，再继续执行后续步骤

## 状态机

```
draft -> planned -> red -> green -> in_review -> ready_to_merge -> done
                                       |               ^
                                changes_requested -> red/in_review

任何状态(除 done) 可进入 blocked，必须回到进入前的状态
```

## 操作顺序

### 完整流程

1. **上下文初勘** — 搜代码、测试、文档、报错，确认影响面与风险
2. **Bootstrap** — `workflowctl ensure`（幂等）
3. **规划 Sprint** — `init-sprint` + `init-sprint-plan`（planner 拆解，批量创建 task）
4. **逐个 Task**：激活 → 写完整 spec → TDD → build/lint/type/unit + 画像验证 → review → `check` → done
5. **Sprint 校验** — `sprint-check`
6. **收尾** — 知识提炼 + `deactivate`

### 轻量流程

1. **上下文初勘** — 先确认这真的是单一变更单元，而不是表面轻量
2. **Bootstrap** — `workflowctl ensure`（幂等）
3. **创建单 Task Sprint** — `init-sprint` + `init-task`（跳过 planner）
4. **激活** → 写完整 spec 模板（可短写，不可缺节） → TDD → build/lint/type/unit + 画像验证 → review → `check` → done
5. **Sprint 校验** — `sprint-check`
6. **停用** — `deactivate`

### workflowctl 命令参考

以下命令默认在项目根目录执行，并使用已安装到 PATH 的 `workflowctl` 命令。优先把 `--project-dir` 传项目根目录 `.`；`--project-dir` 要求项目目录名和 `--project-slug` 精确一致。旧脚本仍可继续使用 `--root <项目父目录>`，如果你手上只有 `.theking/` 路径，传 `--project-dir .theking` 即可。

```bash
# 初始化项目
{theking_cmd} ensure --project-dir {workflow_root_quoted} --project-slug {project_slug}

# 创建 sprint
{theking_cmd} init-sprint --project-dir {workflow_root_quoted} --project-slug {project_slug} --theme <THEME>

# 批量创建 task（推荐）
{theking_cmd} init-sprint-plan --project-dir {workflow_root_quoted} --project-slug {project_slug} --sprint <SPRINT> --plan-file plan.json

# 激活 / 停用 task
{theking_cmd} activate --task-dir <TASK_DIR>
{theking_cmd} deactivate --project-dir {project_dir_quoted}

# 校验
{theking_cmd} check --task-dir <TASK_DIR>
{theking_cmd} sprint-check --sprint-dir <SPRINT_DIR>
```

## Review Loop

- reviewer 产出 `review/code-review-round-NNN.md`（必须包含 `## Context` 和 `## Findings`）
- 修改者产出 `review/code-review-round-NNN.resolved.md`（必须包含 `## Fixes` 和 `## Verification`）
- `ready_to_merge` / `done` 前必须有 round 001 到 `current_review_round` 的全部 review pair
- `requires_security_review: true` 时额外需要 security-review pair
- `web.browser` 画像任务额外需要 e2e-review pair

## Agent 触发表

| Agent | 封号 | 出处 | 触发时机 | 是否必须 |
|-------|------|------|---------|---------|
| planner | 中书令 | 中书省 | 复杂特性规划（3+ stages） | 按需 |
| tdd-guide | 门下侍中 | 门下省 | 新功能、bug 修复 | **必须** |
| code-reviewer | 御史大夫 | 御史台 | 每次代码改动后 | **必须** |
| security-reviewer | 刑部尚书 | 刑部 | auth/input/api 或 backend.http 任务 | 条件必须 |
| e2e-runner | 兵部尚书 | 兵部 | web.browser 任务 | 条件必须 |
| architect | 太傅 | 三公 | 架构决策 | 按需 |
| build-error-resolver | 工部尚书 | 工部 | 构建失败时 | 按需 |
| perf-optimizer | 户部尚书 | 户部 | 性能瓶颈、资源优化 | 按需 |
| doc-updater | 礼部尚书 | 礼部 | Sprint 结束后 | 按需 |
| refactor-cleaner | 吏部尚书 | 吏部 | 代码清理阶段 | 按需 |

### 朝廷架构

```
              👑 皇帝（/decree）
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
   ┌─────────┐ ┌─────────┐ ┌─────────┐
   │ 中书省  │ │ 门下省  │ │ 御史台  │
   │ 中书令  │ │ 侍中    │ │ 御史大夫│
   │(planner)│ │(tdd-    │ │(code-   │
   │ 起草方略│ │ guide)  │ │reviewer)│
   └─────────┘ │先验后行 │ │独立监察 │
               └─────────┘ └─────────┘
        ┌──────────┬──────────┐
        ▼          ▼          ▼
   太傅(architect)  六部执行层
   三公，定大政方针  ├─ 吏部（refactor-cleaner）裁汰冗员
                    ├─ 户部（perf-optimizer）度支钱粮
                    ├─ 礼部（doc-updater）修史编典
                    ├─ 兵部（e2e-runner）全线操演
                    ├─ 刑部（security-reviewer）安全审谳
                    └─ 工部（build-error-resolver）排障修复
```

## Artifact 合同

```text
.theking/workflows/{project_slug}/
├── project.md
└── sprints/sprint-NNN-<theme>/
    ├── sprint.md
    └── tasks/TASK-NNN-<slug>/
        ├── task.md
        ├── spec.md
        ├── review/
        │   ├── code-review-round-001.md
        │   ├── code-review-round-001.resolved.md
        │   └── ...
        └── verification/<profile>/
```
