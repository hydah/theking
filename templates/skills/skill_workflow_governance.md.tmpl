---
name: workflow-governance
description: |
  theking 开发工作流治理 — 状态机、分流决策、硬规则、Phase 1-5 完整编排、review loop、agent 触发。
  触发词：开发工作流、sprint、task、spec、review、TDD、状态机、agent 协作、decree、奉旨开发。
---

# Workflow Governance · 奉旨开发

{project_title} 开发工作流治理。本 skill 包含规则、Phase 1-5 完整编排与 workflowctl 命令序列。

## ⛔ 硬规则 — 9 条门禁，违反任何一条 = 交付无效

1. **创建 sprint 后才能写代码** — 收到任务后，第一个 tool call 不是文件编辑工具（`replace_in_file` / `write_to_file` / `Edit` / `Write` 等）。第一个 tool call 要么是搜索/阅读代码（初勘），要么是 `{theking_cmd} ensure`（建 sprint）。**如果你发现自己在没有 active task 的情况下修改了源代码文件（.go / .ts / .tsx / .js / .jsx / .py / .rs / .java / .css / .html 等），立刻停止，回去建 sprint。** → 落地于 Phase 3
2. **先审上下文，再做分流** — 禁止仅凭需求描述判断。必须先搜索 + 阅读相关代码，输出初勘结果，再宣告流程级别。 → 落地于 Phase 1-2
3. **先写 spec，再写代码** — `spec.md` 的 Scope / Non-Goals / Acceptance / Test Plan / Edge Cases 全部 section 必须填写后，才能开始实现。Acceptance 每条 `- [ ]` 强制挂两行子项 `- 验证方式:` / `- 证据路径:`，由 `{theking_cmd} check` 的 `validate_acceptance_traceability` 校验。 → 落地于 Phase 4 步骤 2
4. **先红后绿（TDD）** — 召 tdd-guide agent 先写测试，再写实现。禁止实现代码先于测试代码存在。 → 落地于 Phase 4 步骤 3-4
5. **每次改动必须 review** — 召 code-reviewer agent。auth/input/api 或 backend.http 任务额外召 security-reviewer。 → 落地于 Phase 4 步骤 7
6. **没有 build/test 通过不进入 review** — 运行 build/lint/type/unit 检查，输出证据。 → 落地于 Phase 4 步骤 5
7. **没有 review pair 不进入 done** — `review/code-review-round-NNN.md` + `.resolved.md` 必须完整。 → 落地于 Phase 4 步骤 8
8. **compact/换工具先跑 `{theking_cmd} status`** — 恢复状态，不要重新发起。 → 见「中断恢复」章节
9. **有启动入口的 sprint 必须有“冷启动真实跑通”证据** — 任何 `backend.http` / `backend.cli` / `backend.job` / `web.browser` 画像的 task，`ready_to_merge` / `done` 前必须在 `verification/<profile>/` 留下**实质性**证据（启动命令 + stdout/stderr 片段 + 至少一条**非 mock** 的请求/交互/观察），由 `has_substantive_verification_evidence` 强制——纯占位符（“TODO” / “待补” / 空 HTML 注释）和 < 40 个实质字符都会被拒。sprint 收尾前必须跑 `{theking_cmd} sprint-smoke` 核查**跨 task 的联调证据**；`seal-sprint` 自动前置调用 sprint-smoke，缺证据不得封印。**不设 `--skip-smoke` 逃生阀**。 → 落地于 Phase 4 步骤 5 + Phase 5 步骤 1, 7

**自检清单 — 在每个 Phase / 步骤转换时强制过一遍：**

```
Phase 1 → 2: 是否已输出初勘结果（已查看/影响面/风险标签）+ 调研清单 4 条全部勾选？
Phase 2 → 3: 是否已运行 {theking_cmd} ensure + init-sprint + init-task？
Phase 3 → 4: task 目录已创建？（activate / spec / planned 在 Phase 4 子门禁里把关）
Phase 4 步骤 2 → 3（spec→TDD）: spec 全部 section 填完？Acceptance 每条带验证方式/证据路径？status=planned？
Phase 4 步骤 3（Red）: 测试先写？运行失败（= 有效的 red）？
Phase 4 步骤 4（Green）: 最小实现？测试通过？status 推到 green？
Phase 4 步骤 5 → 7（verification→review）: build/test + 画像验证都有证据（>= 40 实质字符，非占位符）？
Phase 4 review → done: review pair 完整？check 通过？status 到 done？
Phase 4 → Phase 5: 所有 task 都是 done 或显式 blocked？
Phase 5 内: sprint 级全量回归过了？sprint-smoke 跨 task 联调证据通过？refactor-cleaner 改动后的 build+test+review 都过了？
```

---

## ⚖️ 分流决策（完成最小上下文初勘后）

**核心原则**：所有需求都通过 sprint 跟踪（区别在流程轻重，不在是否跟踪）；先审上下文再分流；轻量流程只减少规划开销，不降低交付标准。

收到任务后，先完成最小上下文初勘，再评估流程级别。最小上下文初勘至少包括：

- 阅读相关代码、测试、文档、错误日志或接口定义中的直接证据
- 标记影响面：文件、模块、接口、数据契约、用户流程
- 识别风险标签：auth、input、api、web.browser、schema/config/public contract
- 输出仍不确定的点；只要存在关键未知项，默认升级到完整流程

### 完整流程

条件：出现以下任一情况就走完整流程：

- 影响面跨 3+ 文件、跨模块或跨团队契约
- 需要架构决策、数据迁移、配置变更或公共接口变更
- 存在多 task 依赖、回滚复杂度高，或关键未知项尚未排清
- 触发高风险画像，且需要多阶段验证才能确认结果

### 轻量流程

只有在完成初勘后，同时满足以下条件，才允许走轻量流程：

- 能确认是单一变更单元，单 task 即可闭环
- 无 schema/config/public contract 变化，无额外架构分叉
- 已有现成实现模式可复用，不需要 planner 拆多 task
- 风险与验证路径明确，不存在关键未知项

若轻量流程执行中发现隐藏耦合、风险扩大或验证不足，必须立即升级为完整流程，不能硬撑到底。

### 机械流程

只有在完成初勘后，同时满足**全部**以下条件，才允许走机械流程：

- 变更纯粹是文件移动 / 重命名 / import path 调整 / 配置修改 / 死代码删除 / 版本号更新 / 纯格式化
- **不引入任何新函数、新类型、新接口签名**
- **不变更任何业务逻辑**（包括 if/for/switch 分支）
- **不变更任何测试逻辑**

机械流程跳过 TDD 红状态（`planned → green` 直达），跳过 security-reviewer，但 code-reviewer 仍然**必须**。若 code-reviewer 在 review 时发现 mechanical task 实际包含了逻辑变更，有权**升档**到 lightweight 或 full，task 回退到 planned 重走。

### 对比表

| 环节 | 完整流程 | 轻量流程 | 机械流程 |
|------|---------|---------|---------|
| 上下文初勘 | **必须** | **必须** | **必须** |
| 创建 sprint + task | ✓ 多 task | ✓ 单 task | ✓ 单 task |
| planner 拆解 | ✓ | 跳过 | 跳过 |
| spec.md | 同模板全部 section，详细填写 | 同模板全部 section，可短写但不得省略 | 仅 Scope + Acceptance，无需 Test Plan / Edge Cases |
| tdd-guide | **必须** | **必须** | 跳过（只需 build green + existing tests green） |
| TDD 状态机 | planned → red → green | planned → red → green | planned → green（跳过 red） |
| build/lint/type/unit + 画像验证 | **必须** | **必须** | **必须**（`go build && go test` 输出即可） |
| code-reviewer | **必须** | **必须** | **必须**（可合并到 bundle review） |
| security/e2e review | 条件触发 | 条件触发 | 跳过（纯移动不改逻辑） |
| review loop | 至少 1 轮，可能多轮 | 至少 1 轮，通常 1 轮 | 至少 1 轮，通常 1 轮 |
| check + sprint-check | **必须** | **必须** | **必须** |
| 知识提炼 | ✓ | 按需 | 不需要 |

**不变的底线**：无论哪种流程，sprint 跟踪、完整 spec、TDD、code-review、基本验证、artifact 校验都不可绕过。

## 🧭 中断恢复

任何新会话、compact 后恢复、或切换到另一个 AI 工具时，第一步都不是重新理解需求，而是先恢复当前 decree / task 状态：

```bash
{theking_cmd} status --project-dir {workflow_root_quoted} --project-slug {project_slug}
```

若 `status` 显示仍有 decree checkpoint 或 active task，顺输出的 next step 续执，不要另起一道新旨。如果已经做出分流结论但还没完成 sprint / task 编排，必须把后续动作落到 decree checkpoint；如果已经有 active task，则优先沿 active task 继续。

## 🔁 状态机

```mermaid
stateDiagram-v2
    [*] --> draft
    draft --> planned
    planned --> red
    red --> green
    green --> in_review
    in_review --> ready_to_merge
    in_review --> changes_requested
    changes_requested --> red
    changes_requested --> in_review
    ready_to_merge --> done
    done --> [*]

    note right of changes_requested
        Phase 4 驳回重议分支硬规则：
        修代码必经 red/green 再回 in_review；
        不得直接 changes_requested → in_review 并标 resolved。
    end note

    note right of done
        任何状态（除 done）可进入 blocked；
        解除后须回到进入前的状态。
    end note
```

---

## 🗺️ 施政次第 · Phase 1-5 完整编排

**凡需求皆先立 sprint，然后动笔。无例外。**

### 🔍 Phase 1: 察情 — 斥候初勘（上下文勘验）

先派斥候探实，后议流程。至少完成以下动作：

1. 遍阅直接相关的代码、测试、文档、报错、接口或数据契约（此谓「案牍」）
2. 勘明影响面：涉及哪些文件、模块、用户流程、外部接口（此谓「波及」）
3. 标记风险画像：是否触发 auth、input、api、web.browser、schema/config/public contract 变化
4. 列出仍不明之处；但有关键未决，一律按完整流程处理

**4.5 深度代码考古（重构/大特性主题时强烈建议）**

当影响面涉及 ≥3 个源文件、或涉及跨模块依赖时，在察情阶段追加一次**结构化的深度分析**，产出沉淀到 `.theking/context/<theme>-analysis.md`：

```
产出要求：
  - 能力 / 职责分类（归纳抽象层级，如"模块只有 N 种核心能力"）
  - 重复代码定位（file:line × N，精确到行号）
  - 调用关系图（谁调了谁，哪些是复制粘贴，哪些是真复用）
  - 上下游映射矩阵（前端 → 后端路由 → 上游服务 → SDK）
  - Open questions（明确列出需要用户确认的未知项）
```

此步骤不是硬规则门禁，但**强烈建议**——它的产出直接喂给 architect（出 ADR）和 planner（分解 task），能显著提升后续规划的质量。产出沉淀在 `.theking/context/` 下，后续 sprint 可复用。

奏报格式固定如下：

```
👑 [decree] 上下文初勘：
- 已查看：<代码/测试/文档/报错>
- 影响面：<模块/接口/用户流程>
- 风险标签：<无 / auth / input / api / web.browser / ...>
- 未决问题：<无 / 列表>
- 调研清单：
  - [ ] 现有代码里找到 3 处先例（列 file:line）
  - [ ] 依赖的外部库 / 官方文档已读过最新版
  - [ ] 识别可复用的测试 helper（列出 helper 名与路径）
  - [ ] 同模块既有的 edge case 测试模式已参照
```

调研清单 4 条都要在动笔前逐条过一遍。若其中任何一条无法凭证据勾选，
视为关键未决，必须升级到完整流程；**不允许通过宣告「轻量流程」来跳过
调研清单**。

**⛔ 未察案牍、未勘波及、未过调研清单，不得擅宣「轻量流程」，更不得动笔改代码。**

---

### ⚖️ Phase 2: 议旨 — 朝堂裁夺（流程级别决策）

已完成初勘，评估流程轻重（判据详见上文「分流决策」）。裁夺结论固定格式如下（`此旨意走<完整|轻量>流程` 二选一）：

**完整流程**（常见信号：3+ 文件、架构决策、> 30 分钟，或存在关键未决）：
```
👑 [decree] 此旨意走完整流程。
理由：<基于上下文初勘的证据>
下一步：召中书令（planner）谋划，创建 sprint。
```

**轻量流程**（唯有已完成初勘且可确认为单一变更单元时方可启用）：
```
👑 [decree] 此旨意走轻量流程。
理由：<基于上下文初勘的证据>
下一步：创建单 task sprint。
```

**⛔ 若仍有关键未决，或施工途中发现隐藏耦合/验证范围扩大，须即刻升为完整流程，不得硬撑。**

裁夺一成，即刻落盘 checkpoint，以防 compact 断续：

```bash
{theking_cmd} checkpoint --project-dir {workflow_root_quoted} --project-slug {project_slug} --phase phase-2-triage --flow <full|lightweight> --summary "<一行旨意摘要>" --next-step "<下一步动作>"
```

---

### 📜 Phase 3: 擘画 — 立纲定目（Sprint 规划，未毕不得动代码）

先执行 Bootstrap（幂等）：

```bash
{theking_cmd} ensure --project-dir {workflow_root_quoted} --project-slug {project_slug}
```

#### 完整流程

1. 召 **中书令**（planner）起草方略，拆解需求为 task 列表
2. 方略里出现以下 4 条信号之**任一条**，**必须**先召 **太傅**
   （architect）出 ADR（写入
   `.theking/context/adr/ADR-NNN-<slug>.md`），再进入 step 3：
   - **跨 module 接口**：方略会引入新的跨模块调用点
   - **引入新依赖**：package.json / pyproject.toml / go.mod /
     Cargo.toml 新增依赖或显著抬升版本约束
   - **公共接口变更**：已发布 CLI flag / 配置 schema / REST endpoint /
     public API 的 signature 变化
   - **数据迁移**：schema 变更、字段迁移、索引重建、需回填脚本

   多条信号命中时仍只出一份 ADR，architect 在 ADR 内分段归纳。
   此规则仅对完整流程生效；轻量流程（单 task sprint）不触发。
3. 确认 task 间依赖关系（DAG）
4. 创建 sprint + 批量创建 task：
   ```bash
   {theking_cmd} init-sprint --project-dir {workflow_root_quoted} --project-slug {project_slug} --theme <theme>
   # 推荐：plan.json 直接放 sprint 目录，方便归档、不污染仓库根
   {theking_cmd} init-sprint-plan \
     --project-dir {workflow_root_quoted} --project-slug {project_slug} \
     --sprint <sprint> \
     --plan-file .theking/workflows/{project_slug}/sprints/<sprint>/plan.json
   ```
5. 输出 Task Overview 表和依赖图，呈御览

**plan.json 路径规约**：plan.json 是 sprint 级资产，应放在 `.theking/workflows/{project_slug}/sprints/<sprint>/plan.json`，随 sprint 一同归档。**不要**放在项目根（会污染 git 或被误提交为顶层脏数据）。`init-sprint-plan` 接受任意路径，但放 sprint 目录是最佳实践。

可选：在 `plan.json` 的每个 task 条目里提供 `scope` / `non_goals` / `acceptance` / `edge_cases` 字符串数组，`init-sprint-plan` 会把它们种子进 spec.md，免得 AI 建完 task 后忘了撰 spec。

**写 plan.json 前先读 `.theking/agents/planner.md`**：那里有 task_type 决策树与兼容矩阵，能帮你一次把 `task_type` / `execution_profile` 选对，避免 `init-sprint-plan` 反复拒执。

#### 轻量流程

1. 创建单 task sprint（跳过 planner）：
   ```bash
   {theking_cmd} init-sprint --project-dir {workflow_root_quoted} --project-slug {project_slug} --theme <theme>
   {theking_cmd} init-task --project-dir {workflow_root_quoted} --project-slug {project_slug} --sprint <sprint> --slug <slug> --title <title> --task-type <type>
   ```

**⛔ 叩阙自检 — 入 Phase 4 前必须全部为真：**
- [ ] `{theking_cmd} ensure` 已执行
- [ ] `init-sprint` + `init-task`（或 `init-sprint-plan`）已执行，task 目录已存在

（`activate` / `spec.md` 填写 / `advance-status planned` 由 Phase 4 步骤 1-2 的子门禁管辖，不在此重复。）

**若任一为假，禁止调用文件编辑工具修改源代码文件（.go / .ts / .tsx / .js / .jsx / .py / .rs / .java / .css / .html 等）。**

`init-sprint` 与 `init-sprint-plan` 都会自动刷新 decree checkpoint（`phase: phase-3-planning`），并在创建完 task 后打印 NEXT STEP 提示。若需手动覆写 summary / next-step，可再显式调用：

```bash
{theking_cmd} checkpoint --project-dir {workflow_root_quoted} --project-slug {project_slug} --phase phase-3-planning --flow <full|lightweight> --summary "<同一条旨意摘要>" --sprint <SPRINT> --task-dir <TASK_DIR> --next-step "<下一步动作>"
```

---

### ⚔️ Phase 4: 领旨 — 百工施为（Task 开发）

按依赖图拓扑排序，无依赖的 task **并行启动**（轻量流程只有 1 个 task）。**每个 task 严走以下主干流程**，状态推进内嵌于每步末尾（`workflowctl check` 只校验结构，不替你推状态）。

#### 🛡️ 主干流程（必做，顺序不可跳）

**1. 启印 · 激活（activate）**
```bash
{theking_cmd} activate --task-dir <TASK_DIR>
```

**2. 撬章 · 写 Spec**
- 完整流程与轻量流程使用同一模板；Scope / Non-Goals / Acceptance / Test Plan / Edge Cases 全部 section 必须保留
- 轻量流程可以短写，但不得留空、不得省略章节
- 撰毕立刻：`{theking_cmd} advance-status --task-dir <TASK_DIR> --to-status planned`

> **⛔ 章表门禁（spec→TDD）— 必须全部为真方可进步骤 3：**
> - [ ] spec.md 所有 section 已填写（不是模板 placeholder）
> - [ ] task status 已推到 `planned`
>
> **未达，禁止调用文件编辑工具修改源代码文件。**

**3. 门下封驳 · TDD Red**
- 召 **门下侍中**（tdd-guide）
- **编译型语言（Go/Rust/Java/C++）额外步骤**：先写 **skeleton**（只有类型定义 + 函数签名 + 零值 return），使测试文件能编译。skeleton 不是实现代码——它是 spec 类型契约的代码化表达。skeleton 规则详见 tdd-guide agent。
- 写一个或一组最小能暴露缺陷的测试，运行应当**失败**（以证其有效）
- **注意**：编译型语言中，"红"状态 = 测试框架运行并报告 FAIL，**不是**编译错误。`go test` 输出 FAIL = 有效红；`go build` 报错 = 回去修 skeleton。
- 测试失败后：`{theking_cmd} advance-status --task-dir <TASK_DIR> --to-status red`

**4. 最小立成 · TDD Green**
- 以**最小**实现使步骤 3 的测试通过，不得擅自外扩
- 测试通过后：`{theking_cmd} advance-status --task-dir <TASK_DIR> --to-status green`

**5. 亲勘实证 · 基础验证 + 画像验证**

运行与改动相关的 build/lint/type/unit checks，并补齐执行画像要求的验证：
- `web.browser`：至少做一次真实页面 smoke 或轻量 e2e
- `backend.http`：至少做一次真实请求或集成式验证。**改了核心逻辑的 task 必须有 L2 级集成测试证据**（起 test server → 发请求 → 断言响应），详见 verification-strategy.md I-014。最低要求是 L1（能建连 + 合法响应 + 正常断连）。
- `backend.cli` / `backend.job`：至少执行真实入口并核对输出或副作用
- 将命令、结果、截图/日志等证据写入 `verification/<profile>/`

> **💡 证据合并（推荐模式）**
>
> 多个 acceptance 条目的证据**推荐合并到一个文件** `verification/<profile>/evidence.md`，用 `##` 分节。校验逻辑按目录聚合所有文件的 substantive 字符数，单文件和多文件等效。合并减少 tool call 次数和 context 占用。
>
> ```markdown
> # Evidence
> ## acceptance-1-name
> $ command-that-was-run
> Expected output observed. Exit 0.
> ## acceptance-2-name
> $ another-command
> Result matches spec. Verified.
> ```

**⛔ 证据闸（硬规则 #9 的 task 级落点）**：
`{theking_cmd} check` 在 `ready_to_merge` / `done` 时会调用
`has_substantive_verification_evidence`——profile 目录下所有文件
的 substantive 字符数（剥注释 / 剥 placeholder token 后的非空白
字符）累加必须 ≥ 40。纯占位符（`TODO` / `待补` / `<!-- ... -->`）、
2 字节随机二进制、bullet-only 内容**全部拒绝**。写 evidence 时
至少给一行真命令 + 一行可验证的输出。

**6. 归档 · 提交（Commit）**
- 按有意义粒度 commit：典型为 `spec + red 测试` 一次、`green 实现` 一次、（可选）`重构` 一次
- **禁止**整个 task 一锅端提交；**禁止**`WIP` 粒度碎 commit

**7. 御史复核 · Review**
```bash
{theking_cmd} init-review-round --task-dir <TASK_DIR>
```
- 召 **御史大夫**（code-reviewer）— 必须
- 召 **刑部尚书**（security-reviewer）— auth/input/api 或 backend.http 时
- 召 **兵部尚书**（e2e-runner）— web.browser 时
- **有 CRITICAL/HIGH findings → 走【驳回重议分支】，不要在当前轮次里直接改代码后标 resolved**
- **全部已解决 → 产出 `round-NNN.resolved.md`（含 `## Fixes` + `## Verification`）**

> **⚡ reviewer 并发（同一 round 内）**
>
> code-reviewer / security-reviewer / e2e-runner 在**同一轮 review** 内可
> **并行召唤** —— 它们消费同一份 git diff，互不依赖，没有理由让 e2e-runner
> 等 code-reviewer 收尾。
>
> **串行只在 review pair 之间（round N → round N+1）保留**：必须先把
> round N 的 `.resolved.md` 写完，再决定是 advance-status 还是开 round N+1。
> 跨 round 并行会破坏审计链。

> **💡 resolved 后下一步：直接 advance-status，不要再 init-review-round**
>
> 当 round N 的 resolved.md 已写完，且**没有**后续代码改动，下一步就是 `advance-status → ready_to_merge → done`，**不要**再跑 `init-review-round` 开 round N+1。再 init 会被 workflowctl 正确地识别为"新一轮 review"、current_review_round 会 +1，导致你必须再写一份空的 round (N+1) review 文件，多走一遍仪式却无新信息 —— 这是实战里 Kimi CLI 反复踩过的坑。**只有在驳回重议分支里改过代码、且 changes_requested 状态下**才走 init-review-round 开新轮。

**8. 盖印 · 终态校验**
```bash
{theking_cmd} check --task-dir <TASK_DIR>
{theking_cmd} advance-status --task-dir <TASK_DIR> --to-status ready_to_merge
{theking_cmd} advance-status --task-dir <TASK_DIR> --to-status done
```

**✅ 终态闸门**：只有 task 为 `done`（或显式 `blocked`）方可进入 Phase 5。`workflowctl deactivate` 会对非终态 task 直接拒执。

#### 🔄 驳回重议分支（review 发现 CRITICAL/HIGH 时必走）

```
a. {theking_cmd} advance-status --task-dir <TASK_DIR> --to-status changes_requested
b. 回步骤 3：若需增/改测试，先让测试重新变红
c. 回步骤 4：修实现使测试重新变绿
d. 回步骤 5：重跑基础验证 + 画像验证
e. 回步骤 6：以 fixup commit 落盘
f. 产出 round-NNN.resolved.md（含 ## Fixes + ## Verification）
g. 若尚有未解决 findings → 再开一轮 init-review-round；否则回步骤 8
```

**⛔ 禁止**：在 review 轮次里直接改代码后把状态从 `in_review` 跳回 `in_review` 并标 resolved，**必经 `changes_requested` → `red`/`green`**。

#### 🚨 其他异常分支

- **构建 / 类型 / lint 失败** → 召 **工部尚书**（build-error-resolver）→ 回步骤 5 重新验证
- **性能瓶颈** → 召 **户部尚书**（perf-optimizer）→ 先在步骤 3 补性能测试（red），再回步骤 4

---

### 🏛️ Phase 5: 缴旨 — 班师覆命（Sprint 收尾）

**⛔ 入 Phase 5 门禁**：本 sprint 所有 task 必须是 `done`（或显式 `blocked` 并载明缘由）。有任一 task 仍在 `red`/`green`/`in_review` 等中间态，**禁止**进入 Phase 5。

#### 1. 📋 点验 · Sprint artifact 校验

```bash
{theking_cmd} sprint-check --sprint-dir <SPRINT_DIR>
{theking_cmd} sprint-smoke --sprint-dir <SPRINT_DIR>
```

`sprint-check` 校验每个 task 的结构合同；`sprint-smoke`（硬规则 #9
的 sprint 级落点）聚合所有 task 的 `execution_profile`，要求每个
profile 在 `sprint-dir/verification/<profile>/` 下都有实质性证据，
缺证据无法 seal。

#### 2. 🧪 演武 · Sprint 级全量回归验证（必做，不可省）

多 task 合并后须再整体演武一次：

- 完整 **build**（所有受影响模块，不是单 task 的局部 build）
- 完整 **lint + type-check**
- 完整 **unit + integration 测试套件**
- 失败 → 定位到具体 task，**回到 Phase 4 对应 task** 修复（状态机从 `done` 回退到 `red`/`green` 须显式推进），修完回本步重跑

全量回归的命令与结果证据写入 `sprint-dir/verification/regression.md`。

#### 3. 🎏 大阅 · Sprint 级画像验证（按需，依 sprint 涉及画像触发）

- sprint 涉及 `web.browser` → 召 **兵部尚书**（e2e-runner）跑一次**跨 task 的端到端场景**（不只是单 task smoke）
- sprint 涉及 `backend.http` → 跑一次关键集成链路（多服务协同、真实依赖）；必须有**至少一个 endpoint 的完整 request-response 证据**（L1 连接级），改核心逻辑的 task 需 L2 业务级证据
- sprint 涉及 `backend.cli` / `backend.job` → 按关键 pipeline 实跑一次

跨 task 端到端证据写入 `sprint-dir/verification/<profile>/`，同一批
证据也会被 `sprint-smoke` 核验（硬规则 #9）——实跑 + 落盘 = 一次动作两个用途。
- 证据写入 `sprint-dir/verification/<profile>/`

#### 4. 🧹 裁汰 · 代码清理（完整流程必做，轻量流程按需）

召 **吏部尚书**（refactor-cleaner）裁汰冗余代码。**注意：refactor-cleaner 会修改源代码，必走完整验证闭环，不得跳过 review。**

```
a. refactor-cleaner 产出删除/重构清单（dead code、重复、过时抽象）
b. 应用改动
c. 再次执行步骤 2（完整 build + test），确认无误删被动态引用之代码
d. 召 code-reviewer 审查清单，重点：反射 / 动态 import / 配置字符串引用 / 外部 API 兼容
e. 有 CRITICAL/HIGH → 回滚或补修；全绿 → 以独立 commit 落盘
```

**⛔ 禁止**：把 refactor-cleaner 的改动直接 commit，不跑 build+test+review。

#### 5. 📖 编典 · 知识提炼

将有复用价值的分析产物提炼到 `.theking/context/`（架构决策、踩坑记录、可复用模式）。

#### 6. 📜 修史 · 文档更新（完整流程必做，轻量流程按需）

召 **礼部尚书**（doc-updater）更新 README / ARCHITECTURE / CHANGELOG / CLAUDE.md 等。

```
a. doc-updater 产出文档变更
b. 召 code-reviewer 做【文档专项轻审】，重点：
   - 文档与实际代码是否一致（接口签名、配置项、命令、默认值）
   - 是否引入过时信息或矛盾
   - 示例代码是否能跑
c. 不一致 → 回炉重写；一致 → commit
```

#### 7. 🔒 封印 · 停用 + Seal

```bash
{theking_cmd} deactivate --project-dir {project_dir_quoted}
{theking_cmd} seal-sprint --sprint-dir <SPRINT_DIR>
```

`seal-sprint` 在所有 task 都为 `done` / `blocked` 时给 sprint.md 顶部
盖一个 `status: sealed` + `sealed_at: <UTC ISO8601 Z>` 的印记。**封印后该
sprint 即为 immutable 审计单元**：`init-task --sprint <sealed>` 与
`init-sprint-plan --sprint <sealed>` 都会拒执，新想法一律走
`followup-sprint`（见下一节）。

可省略：早期 sprint（sprint-001 / sprint-002 之前）的历史 sprint.md 没有
frontmatter，仍可正常读写——不强制回填。

#### 8. 📯 奏捷 · 汇报旨意执行结果

总结本次交付：完成的 task 列表、全量回归结果、知识沉淀指针、遗留 blocked 项。

---

## 🔁 Sprint 收尾后的回补 · followup-sprint

Sprint 一旦 `seal-sprint`，就是 immutable 审计单元。**封印后再有想法、
发现遗留、或想做加强**，一律**新起 sprint**，绝不回填旧 sprint。轻重无
关：完整流程与轻量流程的 sprint 都遵循同一规则。

按"距源 sprint 远近 × 与原 theme 相关性"分四种情况选型：

| 距源 sprint | 与原 theme | 命名建议 | 反向链接 |
|------------|----------|---------|---------|
| **场景 A · 自然延续**：sprint-N 刚收尾 | 同主题加强 | `sprint-(N+1)-<theme>-continued` | 建议但非强制 |
| **场景 A · 独立议题**：sprint-N 刚收尾 | 与原 theme 无关 | `sprint-(N+1)-<new-theme>` | 不需要 |
| **场景 B · 真 bug / 明确遗留**：已迭代多个 sprint 后回看 | sprint-N 留下的明确 bug | `sprint-(N+k+1)-followup-sprint-NNN-<slug>` | **强制** |
| **场景 B · 演进 / 重构**：已迭代多个 sprint 后回看 | 新阶段演进，非 sprint-N 的错 | `sprint-(N+k+1)-<new-theme>` | 在 evolution-workflow-ux 记一笔即可 |

**强制反向链接**的两条机制：

```bash
{theking_cmd} followup-sprint \
  --project-dir . --project-slug <slug> \
  --source-sprint .theking/workflows/<slug>/sprints/<source-sprint> \
  --new-theme <new-theme-slug> \
  --reason "<one-line reason for the followup>"
```

`followup-sprint` 自动做两件事：

1. 在新 sprint.md 的 `## Theme` 之后插入一段 `## Follow-up Source`，
   带上源 sprint 的相对路径与 reason。
2. 在源 sprint 目录下 append 一行到 `followups.md`（首次创建文件，之后
   只追加）；这是元数据追加，不算"改写历史"，对已 sealed 的 sprint 也
   合法（不动 sprint.md 本体）。

**不允许** reopen 已 sealed 的 sprint —— 没有 `reopen-sprint` 命令也
不会有；reopen 会破坏 sprint 是 immutable 审计单元的根基。完整规则见
`.theking/context/evolution-workflow-ux.md` I-007。

---

## 📋 workflowctl 命令索引

所有命令均在项目根目录执行，依赖 PATH 上已安装的 `workflowctl`。优先把 `--project-dir` 传项目根目录 `.`；目录名必须与 `--project-slug` 精确一致。旧脚本仍可用 `--root <项目父目录>`；只有 `.theking/` 路径时传 `--project-dir .theking`。

| 命令 | 出现位置 |
|------|---------|
| `ensure` (幂等初始化) | Phase 3 起手 |
| `init-sprint` / `init-sprint-plan` / `init-task` | Phase 3 |
| `checkpoint` (手动落盘 decree 状态) | Phase 2 / 3 |
| `activate` / `advance-status` | Phase 4 步骤 1-4、8 |
| `init-review-round` | Phase 4 步骤 7 |
| `check` (单 task 结构校验 + substantive-evidence gate) | Phase 4 步骤 8 |
| `sprint-check` (sprint artifact 校验) | Phase 5 步骤 1 |
| `sprint-smoke` (跨 task 联调证据核查，硬规则 #9) | Phase 5 步骤 1；`seal-sprint` 自动前置调用 |
| `deactivate` (停用 active task) | Phase 5 步骤 7 |
| `seal-sprint` (sprint.md 盖印 immutable + 前置 sprint-smoke) | Phase 5 步骤 7 |
| `followup-sprint` (新起回补 sprint + 反向链接) | Phase 5 之后（见「Sprint 收尾后的回补」节） |
| `status` (中断恢复) | 见「中断恢复」章节 |

## 🔁 Review Loop · 复核闭环

- reviewer 产出 `review/code-review-round-NNN.md`（必须包含 `## Context` 和 `## Findings`）
- 修改者产出 `review/code-review-round-NNN.resolved.md`（必须包含 `## Fixes` 和 `## Verification`）
- `ready_to_merge` / `done` 前必须有 round 001 到 `current_review_round` 的全部 review pair
- `requires_security_review: true` 时额外需要 security-review pair
- `web.browser` 画像任务额外需要 e2e-review pair

## 🏛️ 朝廷执事表（Agent 触发规则）

| Agent | 封号 | 出处 | 触发时机 | 是否必须 |
|-------|------|------|---------|---------|
| planner | 中书令 | 中书省 | 复杂特性规划（3+ stages） | 按需 |
| tdd-guide | 门下侍中 | 门下省 | 新功能、bug 修复 | **必须** |
| code-reviewer | 御史大夫 | 御史台 | 每次代码改动后 | **必须** |
| security-reviewer | 刑部尚书 | 刑部 | auth/input/api 或 backend.http 任务 | 条件必须 |
| e2e-runner | 兵部尚书 | 兵部 | web.browser 任务 | 条件必须 |
| architect | 太傅 | 三公 | 架构决策 | 按需 |
| build-error-resolver | 工部尚书 | 工部 | 构建失败时 | 按需 |
| perf-optimizer | 户部尚书 | 户部 | 性能瓶颈、资源优化 | 按需 |
| doc-updater | 礼部尚书 | 礼部 | Sprint 结束后 | 按需 |
| refactor-cleaner | 吏部尚书 | 吏部 | 代码清理阶段 | 按需 |

### 🏰 朝廷架构

```mermaid
flowchart TD
    EMPEROR["👑 皇帝<br/>/decree 或自然语言触发"]

    subgraph SanGong["三公 · 顾问层"]
        TAIFU["太傅<br/>(architect)<br/>架构决策"]
    end

    subgraph SanSheng["三省 · 决策/监察层"]
        ZHONGSHU["中书省 · 中书令<br/>(planner)<br/>起草方略"]
        MENXIA["门下省 · 侍中<br/>(tdd-guide)<br/>先验后行"]
        YUSHI["御史台 · 御史大夫<br/>(code-reviewer)<br/>独立监察"]
    end

    subgraph LiuBu["六部 · 执行层"]
        LI["吏部<br/>(refactor-cleaner)<br/>裁汰冗员"]
        HU["户部<br/>(perf-optimizer)<br/>度支钱粮"]
        LIBU["礼部<br/>(doc-updater)<br/>修史编典"]
        BING["兵部<br/>(e2e-runner)<br/>全线操演"]
        XING["刑部<br/>(security-reviewer)<br/>安全审谳"]
        GONG["工部<br/>(build-error-resolver)<br/>排障修复"]
    end

    EMPEROR --> SanGong
    EMPEROR --> SanSheng
    SanSheng --> LiuBu
```

## 📂 Artifact 合同

```text
.theking/workflows/{project_slug}/
├── project.md
└── sprints/sprint-NNN-<theme>/
    ├── sprint.md
    └── tasks/TASK-NNN-<slug>/
        ├── task.md
        ├── spec.md
        ├── review/
        │   ├── code-review-round-001.md
        │   ├── code-review-round-001.resolved.md
        │   └── ...
        └── verification/<profile>/
```
