# {project_title} Bootstrap

本文件是 {project_title} 面向多个 AI 编程工具共享的入口。

## 两层定位

theking 既是**项目知识库**，也是**工作流治理工具**：

1. **知识库** — `.theking/context/` 沉淀项目分析、架构决策、技术调研等长期知识，随项目一起成长
2. **工作流** — `.theking/workflows/` 管理 sprint / task / review 工件，强制执行状态机和 review 闭环

二者互补：知识库提供上下文，工作流保证执行纪律。

## Source of Truth

| 目录 | 用途 | 生命周期 |
|------|------|---------|
| `.theking/context/` | 稳定项目知识：架构、调研、技术选型 | 随项目持续积累 |
| `.theking/memory/MEMORY.md` | 临时补充记忆，成熟后晋升到 context/ | 定期整理 |
| `.theking/state/session/` | decree checkpoint 与恢复摘要 | 会话中断时更新 |
| `.theking/agents/` | agent 定义（canonical authored source） | 随 agent 能力演进 |
| `.theking/commands/` | 共享命令定义（canonical authored source） | 随工作流演进 |
| `.theking/skills/` | 项目级 skill（canonical authored source） | 随治理规则演进 |
| `.theking/hooks/` | hook 脚本（canonical authored source） | 随治理规则演进 |
| `.theking/prompts/` | 共享 prompt 导出源 | 随命令演进 |
| `.theking/verification/` | 验证画像与证据约束 | 随测试策略演进 |
| `.theking/workflows/` | project / sprint / task / review 工件 | 每个 sprint 产生新工件 |

## Runtime Exposure

以下文件是薄适配层，不维护第二套规则：

- `CLAUDE.md` → 回接本文件（Claude Code 入口）
- `CODEBUDDY.md` → 回接本文件（CodeBuddy 入口）
- `AGENTS.md` → 回接本文件（通用 agent 入口，Kimi CLI 也读这份）
- `.claude/agents/` / `.claude/commands/` / `.claude/skills/` → `.theking/` 下对应 authored source 的 Claude 暴露层（优先软链，失败回退为复制）
- `.codebuddy/agents/` / `.codebuddy/commands/` / `.codebuddy/skills/` → `.theking/` 下对应 authored source 的 CodeBuddy 暴露层（优先软链，失败回退为复制）
- `.kimi/skills/` → `.theking/skills/` 的 Kimi CLI 暴露层（软链，与 Claude/CodeBuddy 共享同一份 SKILL.md）
- `.kimi/AGENTS.md` → 项目根 `AGENTS.md` 的软链（Kimi 会把项目根到工作目录所有 `AGENTS.md` 合并进 `${{KIMI_AGENTS_MD}}`）
- `.kimi/agent.yaml` + `.kimi/agents/*.yaml` → 按 `.theking/agents/*.md` 派生出的 Kimi 原生 YAML agent 定义（`system_prompt_path` 指回 canonical md，编辑 md 即改 Kimi prompt）；用 `kimi --agent-file .kimi/agent.yaml` 启动
- `.github/prompts/` → `.theking/prompts/` 的 GitHub Copilot 导出层
- `.github/skills/` → `.theking/skills/` 的 GitHub Copilot 导出层
- GitHub Copilot 只消费 `.github/prompts/` 与 `.github/skills/`，不导出 `.github/agents/` 或 `.github/commands/`
- `.claude/settings.json` → hooks 配置
- `.codebuddy/settings.json` → hooks 配置
- Kimi CLI 当前官方不支持 lifecycle hooks，治理纪律依赖 `workflow-governance` skill 文本 + `workflowctl` 命令行校验

## 最小读取顺序

1. 先看 `.theking/README.md`
2. 再看 `.theking/context/project-overview.md`
3. 需要架构时看 `.theking/context/architecture.md`
4. 需要开发与验证方式时看 `.theking/context/dev-workflow.md`
5. 只有正式文档未覆盖时才看 `.theking/memory/MEMORY.md`
6. 开发工作流治理：`.theking/skills/workflow-governance/SKILL.md`
7. 知识沉淀规范：`.theking/skills/knowledge-base/SKILL.md`

## 开发工作流底线

- 先做上下文初勘，再决定完整流程还是轻量流程。至少查看相关代码、测试、文档、报错或接口契约中的直接证据。
- 轻量流程只减少规划开销，不减少交付要求。spec、TDD、build/lint/type/unit、执行画像验证、code review、check/sprint-check 都不能跳过。
- `init-task` 生成的 `spec.md` 是占位稿。任务可先停在 `draft` 或 `planned`，但进入 `red` 前必须补全 Scope、Non-Goals、Acceptance、Test Plan、Edge Cases 五段内容。

错误示例：

```text
👑 [decree] 此旨意走轻量流程。
理由：预计只改 1-2 个文件。
```

正确示例：

```text
👑 [decree] 上下文初勘：
- 已查看：<代码/测试/文档/报错>
- 影响面：<模块/接口/用户流程>
- 风险标签：<无 / auth / input / api / web.browser / ...>
- 未决问题：<无 / 列表>

👑 [decree] 此旨意走<完整|轻量>流程。
理由：<基于已查看证据的判断>
```

## Compact Recovery

compact、换工具、或长时间中断后，先运行：

```bash
workflowctl status --project-dir . --project-slug {project_slug}
```

如果 decree 已经做出分流结论但还没完成 sprint / task 编排，用 checkpoint 持久化后续动作：

```bash
workflowctl checkpoint --project-dir . --project-slug {project_slug} --phase phase-2-triage --flow <full|lightweight> --summary "<一行摘要>" --next-step "<下一步动作>"
```

如果已经进入具体 task，则 `status` 会优先告诉你 active task 的 status、review round 和 next step。只有当它明确显示没有 active task、没有 checkpoint、也没有未完成任务时，才开始一条新的 decree。

## 知识沉淀约定

- 项目分析、技术调研、竞品对比 → 写入 `.theking/context/`
- 架构决策 → 写入 `.theking/context/architecture.md` 或单独 ADR 文件
- 临时发现、待确认事项 → 写入 `.theking/memory/MEMORY.md`
- 确认后的 memory 条目 → 晋升到对应 `context/` 文件
- Sprint 过程中的分析产物 → 随 task 沉淀在 task 目录，通用部分提炼到 `context/`

## 首次使用检查

如果 `.theking/context/` 下的文件包含 `<!-- PLACEHOLDER -->` 标记，说明知识库尚未填充实际内容。
请主动分析项目源码、配置文件和目录结构，用分析结果替换所有 PLACEHOLDER 注释。

具体流程：
1. 扫描项目顶层结构和关键配置文件（package.json / pyproject.toml / Cargo.toml / Makefile / Dockerfile 等）
2. 按 PLACEHOLDER 注释中的指令逐项分析并填写
3. 填充后删除 `<!-- PLACEHOLDER: ... -->` 注释
4. 无法确认的发现写入 `.theking/memory/MEMORY.md`，不写入 `context/`
