# {project_title}

> **Canonical source**: [`.theking/bootstrap.md`](.theking/bootstrap.md)
>
> This file is the entry adapter for generic AI agents. For the full project knowledge base, read the `.theking/` directory.

## Quick start

1. Read `.theking/bootstrap.md` for the full context index
2. Read `.theking/context/project-overview.md` for project scope
3. Read the task's `spec.md` before writing any code
4. Follow the state machine: draft → planned → red → green → in_review → ready_to_merge → done

## Governance

This project uses [theking](https://github.com/theking) workflow governance:

- Every task must have `task.md` + `spec.md` + `review/` directory
- No implementation before spec is written
- No merge without review pairs
- Agent definitions are in `.theking/agents/`

## Do NOT maintain rules here

All project rules live in `.theking/`. This file only points there.
