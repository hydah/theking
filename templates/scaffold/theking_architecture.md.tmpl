# Architecture

## 总览

<!-- PLACEHOLDER: 分析项目顶层目录结构和主要模块，用 3-5 句话描述核心架构、数据流和部署边界 -->

## 目录结构

<!-- PLACEHOLDER: 运行 ls 或 tree（depth=2），列出关键目录并注释用途 -->
```text
项目根/
├── ...
```

## 关键模块

<!-- PLACEHOLDER: 扫描源码目录，识别 3-5 个关键模块，填写下表 -->
| 模块 | 路径 | 作用 | 关键依赖 |
|------|------|------|---------|
| <!-- PLACEHOLDER --> | | | |

## 数据流

<!-- PLACEHOLDER: 描述从用户请求到响应的主要数据流路径，或核心处理管线 -->

## 风险点

- 不要让运行时目录演化成第二套规则
- 不要把临时输出写进 `context/` 或 `memory/`
<!-- PLACEHOLDER: 补充项目特有的架构风险点 -->
