# Dev Workflow

## 本地开发

- 先确认项目依赖与启动方式
- 改动规则、task 合同或 review 约束时，优先更新 `.theking/` 中的 canonical docs

## 验证策略

### web.browser

- 启用浏览器或 Playwright
- 验证关键用户路径、控制台错误、页面截图或 trace

### backend.http

- 启动服务后做黑盒 HTTP 请求
- 覆盖核心 API 路径、错误码和依赖交互

### backend.cli

- 以子进程方式运行 CLI 或脚本
- 验证退出码、stdout/stderr、输出文件

### backend.job

- 触发 worker / job 执行
- 验证产物、状态变更或下游副作用

## workflowctl

从 theking 源仓库或你 vendored 的包装入口执行这些命令，`--root` 指向目标项目父目录，也就是 workflow root：

```bash
python3 <path-to-theking>/scripts/workflowctl.py init-project --root <workflow-root> --project-slug {project_slug}
python3 <path-to-theking>/scripts/workflowctl.py init-sprint --root <workflow-root> --project-slug {project_slug} --theme foundation
python3 <path-to-theking>/scripts/workflowctl.py init-task --root <workflow-root> --project-slug {project_slug} --sprint sprint-001-foundation --slug demo --title "Demo" --task-type frontend --execution-profile web.browser
python3 <path-to-theking>/scripts/workflowctl.py check --task-dir <workflow-root>/{project_slug}/.theking/workflows/{project_slug}/sprints/sprint-001-foundation/tasks/TASK-001-demo
```